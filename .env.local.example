# Public client configuration only. Copy to .env.local and fill from
# `supabase status` after starting the local stack, or from the Dashboard
# API Keys tab (Publishable key) for a hosted project.
APP_ENV=development
SUPABASE_URL=http://127.0.0.1:55421
SUPABASE_PUBLISHABLE_KEY=replace-with-local-publishable-key
