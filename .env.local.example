# Public client configuration only. Copy to .env.local and fill from
# `supabase status` after starting the local stack.
APP_ENV=development
SUPABASE_URL=http://127.0.0.1:55421
SUPABASE_ANON_KEY=replace-with-local-anon-key
