# Prompt 00: Bootstrap
Branch: `feature/bootstrap`

Make this repository a runnable Flutter project without changing the product contract. Generate missing iOS, Android and web platform files, initialize Supabase local development, wire environment loading safely, initialize Supabase in Flutter, add routing and Riverpod, and create a minimal smoke test. Keep the existing warm light visual direction. Do not add business features yet.

Acceptance: `flutter analyze` and `flutter test` pass; README setup commands are correct; no secrets are committed. Finish the mandatory Git workflow.
