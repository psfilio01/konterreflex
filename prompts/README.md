# Implementation sequence

Run prompts in numeric order. Each prompt is intentionally scoped and relies on `AGENTS.md` plus the referenced architecture docs.

If Codex finds a materially better implementation that preserves product behavior and architecture boundaries, it may use it and document the deviation in the PR.

Do not combine multiple numbered prompts in one branch unless explicitly requested.
