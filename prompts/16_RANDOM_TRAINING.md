# Prompt 16: Direct adaptive training

Branch: `feature/random-training-flow`

Replace the public training list with a calm resolver that selects the next
curated item through the adaptive practice engine. Start the selected scene
automatically once, and make the next adaptive scene the primary result action.
Keep retry, same-scene repetition, follow-up and Golden Book actions available.

Acceptance: opening Training never displays the catalogue; active content in
the current language is selected; autoplay happens once; loading, empty and
retry states are localized; back navigation does not launch another scene.
Finish the mandatory Git workflow.

