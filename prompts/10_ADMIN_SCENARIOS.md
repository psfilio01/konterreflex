# Prompt 10: Admin scenario studio
Branch: `feature/admin-scenarios`

Build the separate Flutter web admin entry point. Admins can create, edit, preview, approve, archive and reject scenarios. Add batch AI generation, including requests such as “create 50 scenarios”, with generation into draft only. Provide filters and a fast review queue. Actor voice selection and preview belong here.

Acceptance: non admins cannot access admin data or routes; generated scenarios never become active automatically; bulk actions are auditable. Finish the mandatory Git workflow.
